import React from 'react'

export default function RootComponent() {
  return (
    <section>
      <h3>Root component file</h3>
      <p>
        The root component (often `App`) is the top-level component for a page or
        application. It usually imports and composes child components.
      </p>
    </section>
  )
}