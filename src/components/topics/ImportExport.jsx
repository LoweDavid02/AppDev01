import React from 'react'

export default function ImportExport() {
  return (
    <section>
      <h3>Importing and exporting components</h3>
      <p>
        Split components into separate files, export them (default or named),
        and import where needed. This improves reuse and readability.
      </p>
    </section>
  )
}